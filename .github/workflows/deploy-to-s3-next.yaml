name: Deploy to S3 Next
on:
  push:
    branches:
      - next

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      AWS_S3_BUCKET_NAME: webduino-chat
    steps:
      - uses: actions/checkout@master

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version-file: ".tool-versions"

      - name: Cache npm packages
        uses: actions/cache@v2
        with:
          path: "**/node_modules"
          key: ${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-

      - name: Install Dependencies
        run: npm ci

      - name: Build Application
        run: npm run build

      - uses: jakejarvis/s3-sync-action@master
        name: Upload App to S3 Bucket
        with:
          args: --follow-symlinks --delete --exclude '.git/*' --exclude 'node_modules/*'
        env:
          AWS_S3_BUCKET: ${{ env.AWS_S3_BUCKET_NAME }}
          AWS_ACCESS_KEY_ID: "AKIAY4GNWIPH3Y3VMI5I"
          AWS_SECRET_ACCESS_KEY: "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"
          AWS_REGION: "ap-northeast-2"
          SOURCE_DIR: "dist"
          DEST_DIR: "next"
